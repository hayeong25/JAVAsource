package classtest;

// 접근제어자 (public, private) class (클래스명을 class로 지정 불가) 클래스명(클래스 이름은 대문자로 시작)

public class Student {
	
	// 속성(특성) >> 멤버 변수(property)
	// 학번, 이름, 주소, 연락처
	int studentId;
	String studentName;
	String address;
	String mobile;
	
	// 기능(동작) >> 멤버 메소드
	void changeMobile() {
		
	}
	
	void changeAddress() {
		
	}
}